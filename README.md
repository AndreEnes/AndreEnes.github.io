# André Enes - Portfolio

This is my attempt to get some more visibility on my portfolio 🦚.

## Run Locally

1. Install Docker.
2. Run `docker compose up --build` to build the container for the 1st time.
3. Run `docker compose up`.
4. The static page will open in `http://localhost:4000`.
